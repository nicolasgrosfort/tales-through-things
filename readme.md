# Tales Through Things

## Dependencies

### Docker

The project uses Docker. To install it, run the following command:

```bash
    brew install --cask docker
```

### Yarn

The project uses yarn. To install it, run the following command:

```bash
    brew install yarn
```
## Start the project

```bash
    make init 
    make dev
```